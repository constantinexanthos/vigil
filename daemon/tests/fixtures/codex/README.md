# Codex fixture

Captured from: synthetic (no Codex installation found)

If the discovery logic in `codexlog::discover_root()` needs to change, update
the `CANDIDATES` list in `daemon/src/codexlog.rs`.
